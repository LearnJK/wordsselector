from app import app,socketio
if __name__ == "__main__":
	port = 5000
	socketio.run(app)

# export FLASK_ENV=development && flask run

# cada usuario crea la aplicacion desde 0
# primero selecciona un nombre
# la app crea lo siguiente

# cargar los controladores de la aplicacion en el init
# crear el archivo dentro del controlador
# crear el archivo del modelo

# crear el css para los estilos base de la app

# crea la carpeta dentro de tamplates
# crea el archivo html dentro de la carpeta

# agregar la clase makereport

# Creo mi pagina nueva en la carpeta templates
# copio el template layoput dentro de mi archivo y remplazo el contenido
# Agrego mi link en la lista del nav
# Creo mi css para esa pagina
